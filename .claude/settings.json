{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash(git commit*)",
        "hooks": [
          {
            "type": "command",
            "command": "scripts/check-pii.sh && cargo test --workspace --quiet 2>&1 && cargo clippy --all-targets --quiet -- -D warnings 2>&1"
          }
        ]
      }
    ]
  }
}
