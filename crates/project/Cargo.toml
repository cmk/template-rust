[package]
name = "project"
edition.workspace = true
version.workspace = true
rust-version.workspace = true
license.workspace = true
publish = false

[features]
default = ["core"]
core = ["dep:core_impl"]

[dependencies]
core_impl = { package = "project-core", path = "../core", optional = true }
