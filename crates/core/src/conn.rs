//! layer: conn
//! depends-on:
//!
//! Core connection/value boundary layer.
//!
//! This template starts with an intentionally small production layer so
//! downstream projects can extend the layer DAG with domain-specific
//! modules without changing the checker shape.
